# ============================================================================
# RENDER DEPLOYMENT - LIGHTWEIGHT REQUIREMENTS
# ============================================================================
# This file contains ONLY the dependencies needed for Render deployment.
# Heavy ML libraries (PyTorch, transformers, etc.) are NOT needed because
# all AI processing is done by Ollama server (accessed via API).

# FastAPI and server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==14.1

# Pydantic (data validation)
pydantic==2.10.6
pydantic-settings==2.7.1

# Database
psycopg2-binary==2.9.10

# PDF Processing (lightweight)
PyMuPDF==1.26.7
pdfplumber==0.11.9

# HTTP Requests (for Ollama API calls)
requests==2.32.3

# Utilities
python-dotenv==1.0.1
python-dateutil==2.9.0

# Data processing
numpy==2.2.3
pandas==2.2.3

# Export formats
openpyxl==3.1.5

# Yahoo Finance
yfinance>=0.2.40
